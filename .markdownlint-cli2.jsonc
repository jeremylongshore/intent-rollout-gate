// markdownlint-cli2 config for intent-rollout-gate
//
// GitHub Action shell + design/architecture docs — tables and code fences are
// heavy; long lines are common in normative spec text. Tune rules to match.
{
  "config": {
    "default": true,
    "MD013": false, // line-length — specs use long lines for cross-references
    "MD024": { "siblings_only": true }, // allow duplicate headings in different sections
    "MD025": { "front_matter_title": "" }, // frontmatter `title:` doesn't count as the H1
    "MD033": false, // inline HTML allowed (badges, details/summary)
    "MD041": false, // first line need not be H1 (frontmatter or banner first)
    "MD007": { "indent": 2 }, // unordered list indent
    "MD026": { "punctuation": ".,;:!?" }, // trailing punctuation in headings — allow ?
    // MD060 (table-column-style) — OFF. markdownlint v0.40.0 added this rule, and it
    // arrives with markdownlint-cli2-action v23, the lowest major off the node20
    // Actions runtime. It polices pipe spacing in tables, which Prettier already
    // formats, so enforcing it means fighting the formatter on every table. Pure
    // cosmetics, same class as MD013; disabled estate-wide (lab, j-rig, harness).
    "MD060": false,
  },
  "globs": ["**/*.md"],
  "ignores": [
    ".audit-harness/**",
    "node_modules/**",
    "research/phase-a-0-baseline/results/raw/**",
  ],
}
