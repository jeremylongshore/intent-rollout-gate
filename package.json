{
  "name": "intent-rollout-gate-action",
  "version": "0.2.0",
  "private": true,
  "description": "GitHub Action shell for the Intent Rollout Gate. All decision logic is delegated to @intentsolutions/rollout-gate (thin shell per Blueprint A).",
  "license": "Apache-2.0",
  "author": "Jeremy Longshore (Intent Solutions)",
  "engines": {
    "node": ">=20"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run typecheck && npm run test",
    "dist:check": "npm run build && git diff --exit-code dist/"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@intentsolutions/core": "^0.6.0",
    "@intentsolutions/rollout-gate": "2.0.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@intentsolutions/audit-harness": "^1.1.8",
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
